source "https://rubygems.org"

# Matches the Jekyll + plugin versions GitHub Pages builds with, so a local
# `bundle exec jekyll serve` behaves the same as the live site.
gem "github-pages", group: :jekyll_plugins
