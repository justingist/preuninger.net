---
layout: post
title: "Untitled draft"
excerpt: "One or two sentences that summarise the post."
read_time: N min read
---

Start writing here. Standard Markdown — paragraphs, `#`/`##` headings, `> blockquotes`,
`` `inline code` ``, fenced code blocks, and links — all pick up the site's styling
automatically via the post layout.

## A subheading

More body copy.

To publish this post:

1. Move this file into `_posts/`.
2. Rename it `YYYY-MM-DD-your-slug.md` (the date in the filename sets the publish date and
   sort order — the title itself can be anything).
3. Fill in the real `title` and `excerpt` in the front matter above.
4. Commit and push — it will appear at the top of `/blog/` automatically.
